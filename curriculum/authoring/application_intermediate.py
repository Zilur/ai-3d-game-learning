"""逐课真实情境：中级。延续同一庭院，不扩成大型游戏。"""
SCENARIOS = {
'I01': dict(place='P06｜把胶囊外观换成现成角色', before='角色看起来站好了，但比例、前向或骨架与控制器不匹配。', after='新角色可用，原碰撞和控制保持；不合适资源有明确淘汰理由。', task='依据我提供的许可信息和角色文件，先检查骨架、朝向、比例及动画片段，再只替换视觉层。试用一段待机，不承诺任意骨架兼容；复杂坏资产先比较更换成本。', keep='角色控制根、碰撞形状、相机和原输入保持；不要求手工重绑。', tweaks=[('视觉包装Node3D Transform（内置）','只在独立视觉包装校准位置/朝向与比例','脚是否落地、脸是否朝向运动；不缩放物理根来救外观'),('Godot动画列表/预览（入口）','先只播放一段待机','骨架是否变形、是否异常位移')], accept=['能选出可用角色并解释淘汰另一个候选的原因。','能校准外观且复测原控制、碰撞、相机，保留来源信息。'], regress='原走跑跳与拾取路径继续通过；材质替换不改变奖励逻辑。', transfer='第二个风格相似角色重复验收，不能仅因预览漂亮就采用。', risk='检查AI是否缩放整个控制器、修改绑定姿态或把缺失动画误报为兼容。'),
'I02': dict(place='P06｜角色停止后仍在跑，跳落地切换不顺', before='现成动作可播放，但状态条件与真实运动脱节。', after='待机、走跑、起跳和落地按实际条件切换，异常可定位。', task='只把已经验证的动作片段接到已有运动状态。先画输入/速度/落地到动画的条件表，再接状态；提供起停、跑跳、空中松键的连续测试，不靠根运动重写控制器。', keep='移动速度、物理位置来源、相机和碰撞不变。', tweaks=[('AnimationTree过渡时间（内置入口）','记录基线，少量调整同一组过渡','突然切换是否减轻，不能用超长混合遮掉错误状态'),('AnimationPlayer播放速度（内置）','在同一路线比较小幅调速','脚滑与身体速度的对应，不改变实际走速蒙混')], accept=['能用条件表说明为什么此时播放这个动作。','能识别条件错误与过渡不舒服的区别，再局部修改并复测。'], regress='站立、移动、跳跃的物理结果保持；重复切换不累积异常偏移。', transfer='把走路片段换成同骨架新片段，重新调过渡而不复用旧结论。', risk='检查AI是否把动画视觉状态当落地物理事实，或让根位移与控制器双重驱动。'),
'I03': dict(place='P06｜庭院两盏灯：只让其中一盏变化', before='复制物体后修改一处，另一处意外改变；或为隔离一点参数复制全部资源。', after='外观需要共享的继续共享，个体状态与目标材质可独立。', task='针对现有两盏灯列出节点状态与共享资源引用。仅分离需要独立的材质或配置，不深拷贝全部纹理；让亮灭状态属于各自实例，附两个对象的影响范围对照。', keep='其他共享资源、纹理文件、游戏计数不变。', tweaks=[('资源引用与Make Unique等入口（内置）','先确认共享，再对目标资源独立化','一盏改变时另一盏是否仍保持基线'),('每实例状态/导出配置（自定义）','只切换目标实例的亮灭','材质共享与对象状态是否被混为一谈')], accept=['能说明哪些数据属于实例，哪些资源有意共享。','能实现需要的独立变化，同时避免复制不必要的大资源。'], regress='其他灯与主光保持；重载场景后的共享策略一致。', transfer='从两盏灯迁移到两颗果实，一颗被拾取不能影响另一颗状态。', risk='检查AI是否把场景实例化当成所有资源自动独立，或用全量复制解决细小差异。'),
'I04': dict(place='P06｜庭院侧门或一块移动平台', before='门轴对但反复触发失控；平台只看起来运动，站上去行为异常。', after='选定一个小机关，在重触发、站立/离开和重开时可解释。', task='从门或平台中只选一个，先描述允许状态和物理行为。复用已学支点/触发/状态实现最小版本；平台按现有引擎的适用物理体处理，不只Tween可见网格。', keep='玩家控制与原收集循环保持；不同时增加机关系统和敌人系统。', tweaks=[('机关行程或开门角（自定义）','先小行程核对，再调目标角度或距离','碰撞与可见外观是否一起运动'),('机关持续时间（自定义）','固定行程，单独改时长','角色可否安全通过，重触发是否堆叠')], accept=['能复用空间、触发和状态说明一条机关行为链。','能测试重复触发与恢复；选平台时再测站立/离开，不强制两种都做。'], regress='原玩家起跳、墙碰撞、星星收集和重开不退化。', transfer='门变为抬起的栅栏，解释哪个空间规则改变、哪个状态规则保留。', risk='检查AI是否只移动Visual、每次进入都新建一个Tween，或通过禁用碰撞掩盖穿插。'),
'I05': dict(place='P06｜让庭院短围廊拼得齐、纹理不拉伸', before='模块之间有缝，墙面格子大小混乱或被拉长。', after='两三段模块按统一接口拼接，简单UV问题可定位修复。', task='从已有基底准备直段和转角。列出尺度、原点和吸附约定，先用棋盘格诊断纹理；只修简单面上的UV，不重做整套贴图。', keep='已有通行宽度与碰撞职责保持；不拿高分辨率贴图掩盖UV错误。', tweaks=[('Transform/吸附步长（内置）','按约定宽度摆模块，先核对接缝','外观和功能空间是否都对齐'),('Blender UV Editor缩放/位置（内置）','用格子图只调整一块简单面UV','格子是否接近相同尺度，有无拉长')], accept=['能用尺度与原点规则拼接一段短围廊。','能用棋盘格定位简单UV问题并比较修改前后。'], regress='人能通过原路线，镜头不过度被接缝遮挡；重导入仍保留包装。', transfer='换成屋顶或地板模块，沿用接口检查但重新判断贴图比例。', risk='检查AI是否不同模块使用不同单位、随意移动原点或只升纹理分辨率。'),
'I06': dict(place='P06｜美术预览很美，进入可玩庭院却不一致', before='采用了不适合目标设备或动态角色的照明方案。', after='选择一个可实现的光照方案，明确静态和动态对象的限制。', task='按提供的设备目标、渲染器和动态对象清单比较两个可行方案，先说明支持条件。选一个最小样本验证，不启用全部GI；不要假称Blender预览就是游戏效果。', keep='同机位、同资产、同路线，比较时不偷偷改曝光与材质。', tweaks=[('主光/环境强度（内置）','在已选方案下先调少量原生参数','角色暗部与路径是否可读'),('阴影开关/距离等（内置，具体入口按版本查）','只改变一项，再回到同一路线','阴影收益、动态物体一致性和实际成本')], accept=['能基于动态需求与目标设备选出一种方案并说明边界。','能提供固定条件的视觉与运行对照，不仅凭静帧选方案。'], regress='移动角色经过暗区、门开启、物品拾取时表现合理，旧玩法仍通过。', transfer='把一个静态道具改为会移动，重新检查当前方案是否适用。', risk='检查AI是否混淆渲染器支持、烘焙结果与动态照明，或把多加灯当唯一办法。'),
'I07': dict(place='P06｜继续优化还是已经够用', before='因为术语听起来高级就加入批量渲染或剔除，复杂度增加却没有证据。', after='对一个瓶颈假设有数据支持，达到目标后可以停止。', task='阅读我提供的真实测量，先判断证据是否足够，再选一个低成本验证。不能预设一定是Draw Call；给结果不支持假设时的撤销方案，不直接引入大场景框架。', keep='同设备、版本、分辨率和玩法验收条件保持。', tweaks=[('Profiler与监视器（入口）','看指定慢帧区间并重复采样','问题是否稳定，是否可能来自其他系统'),('一个被怀疑的装饰组开关（实验自定义）','只切换目标组，不同时调多个质量选项','变化是否支持假设，以及失去什么画面内容')], accept=['能提出与实际现象相关的瓶颈假设和验证方法。','能根据对照决定继续、撤销或停止，并保留功能/视觉证据。'], regress='没有优化掉交互对象或碰撞；同路线玩法和可读性保持。', transfer='目标设备变弱时重新设约束，不能直接照搬上次达标结论。', risk='检查AI是否把平均FPS提升当成卡顿已消除，或给未测优化收益百分比。'),
'I08': dict(place='P06可选｜庭院巡逻角色能否绕过门', before='路径线存在，但角色穿墙、卡门或到不了目标。', after='一个简单巡逻对象与真实可走区域一致；纯机关路线可跳过。', task='只在选了导航路线后，为庭院复制测试场景加一个巡逻对象。分别检查导航路径与物理移动、门的开闭；不手写寻路算法，也不为此改变主线收集目标。', keep='主玩家和主线收集循环不动；先在副本验证。', tweaks=[('导航代理/导航网格相关半径（内置入口，核对实际属性）','按角色体积检查窄门可达性','路径是否容得下实体，而不仅看细线'),('巡逻速度（自定义）','只改速度，门宽不变','转角实际运动和卡住现象')], accept=['能区分路径规划和真实物理通行，并验证一个门场景。','能按导航路线需求调整一次并复测；未选路线不要求此证据。'], regress='巡逻不会推坏主玩家或改变星星奖励；可完整移除选修对象。', transfer='关上一条通道后重新观察路径与实际结果，不只重新画路径线。', risk='检查AI是否只演示路径可视化、不执行实际移动，或把避障当作物理防穿墙。'),
'I09': dict(place='P06｜靠近门出现提示，按交互才打开', before='远处也能开门，或一个对象直接乱改其他对象的UI。', after='提示、条件、目标和动作结果明确归属，交互只作用当前目标。', task='复用门和已有输入，准备距离/目标/状态检查，只给当前对象显示提示。定义交互请求和结果，不引入通用背包；解释切换目标时谁清理旧提示。', keep='拾取自动检测和门的交互按键不混合；全局计数与玩家运动不变。', tweaks=[('交互距离（自定义或射线长度）','固定视角，改变允许距离','提示出现范围与真正可执行范围是否一致'),('提示位置/字体（Control内置）','固定目标再调整视觉位置','是否挡住门或与收集提示混淆')], accept=['能区分提示可见与条件满足，说明请求由谁处理。','能切换目标、超出范围、重复按键并验证状态不串。'], regress='原自动拾取继续正常；UI不残留上一扇门提示。', transfer='把门换成可读告示牌，保留检测思路但更换结果职责。', risk='检查AI是否只检查距离不检查目标、每个对象直接改全局UI，或提示和行为范围不一致。'),
'I10': dict(place='P06｜退出庭院后恢复必要进度与设置', before='能写文件，但缺失或损坏数据会导致无法进入游戏，或覆盖唯一原档。', after='正常、缺失、旧版和损坏样例都有可说明的恢复策略。', task='先列必须保存的数据及默认值；用给定样例验证加载分支，再生成最小保存实现。损坏样例使用副本，不覆盖唯一原档；不做数据库或云同步。', keep='用户原存档与未授权目录不动；测试数据和正式数据分离。', tweaks=[('设置界面音量/输入映射（项目界面）','修改一个设置后重启','持久化结果与当前界面是否一致'),('给定存档样例选择（教学控件，不是内置按钮）','分别载入正常/缺失/旧版/损坏副本','默认回退、错误提示、原文件保留')], accept=['能选择必要保存内容，解释默认值和恢复行为。','能用样例验证恢复，说明失败证据与原档保护。'], regress='加载后收集、完成、重开仍一致；恢复不会重复发奖励。', transfer='增加一个新设置字段，检查旧档不含它时是否安全。', risk='检查AI是否只测试保存成功、不验证加载，或静默覆盖损坏原档。'),
'I11': dict(place='P06｜把庭院交给没装编辑器的人', before='作者电脑能运行，却不知道缺哪些资源和初始数据。', after='选定平台有可启动、可完成、可恢复的交付记录。', task='依据我选定的平台和当前工程，列出导出所需步骤与最小新用户测试。先核对引用和资源来源，不上传任何密钥；未实际运行不能写已验证。', keep='源项目、唯一存档和第三方原资源不被清理；不扩成多平台商店发布。', tweaks=[('Godot Export Preset（内置入口）','核对单一目标与所需导出资源','是否误漏资源或包含开发秘密'),('游戏画面/音量设置（项目界面）','在干净用户数据下确认首次默认值','首次进入能否看清、操作并完成')], accept=['能列出目标平台、资源使用条件和首次启动检查。','能给出从启动到完成/恢复的真实交付证据或准确报告阻塞。'], regress='导出版本而非编辑器内重走核心循环，记录实际设备与版本。', transfer='换另一个干净用户目录再测，不要求新增第二个平台。', risk='检查AI是否把导出命令成功当可玩验收，或漏掉资源授权和秘密文件。'),
'I12': dict(place='P06｜小庭院中级版本验收', before='加了角色和机关但完整流程不稳定，改动难以解释。', after='正式角色、一个小扩展、必要恢复与交付构成可完成的小型原型。', task='作为审核助手，只按已选路线检查现有版本。整理功能、可读性、性能、恢复四类证据与缺口，先修阻塞项，不借结业再扩功能。', keep='不同时强制导航和机关；不要求从零绑定或新增战斗。', tweaks=[('优先级最高的一项手感参数（按现有项目）','只对照一个高影响问题','是否帮助玩家完成当前目标'),('一项视觉主次设置（已有参数）','固定机位比较，再实际走路线','美化是否影响目标辨识')], accept=['能解释角色、交互、状态和资源的分工及已选扩展。','能交付完整流程与四类证据，对未通过项给可复现说明。'], regress='基础星星循环、角色动画、已选机关/导航、保存恢复和导出检查。', transfer='给出一个小需求变更，判断影响范围再修改，不重做整个游戏。', risk='检查AI是否用更多功能掩盖未完成验收，或把课程全部读过当作掌握。'),
}
