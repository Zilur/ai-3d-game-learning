# 分支整合记录

用户要求把内容合并到main，减少分支。本次不新建分支；在原PR #1完成内容级冲突处理，再合入main。

## 采用哪些内容

- 课程与深度：采用Focus v3的32组概念、初/中/高级路线、M/K、0–2评分、assessments题库、64道跨阶段P/T及K用途题。来源提交：b7f2b5b71d2d73e38208de82a3a1696df60cb035。
- 工程与检查：采用PR #2的Godot走跑跳、10星拾取、空间/门轴实验、50项行为测试、Blender生成脚本和UV诊断Shader。来源提交：c06a0db763ab3c32d7200ce6c7ae51af41b957a2。整合后重新运行，不借用旧结果。
- 保留PR #1的离线网页、双球材质对照、glTF星星和生成脚本。OBJ用于参考关卡；glTF用于资产往返练习，两者用途明确。
- 不并列保留另一套assessment目录、26组概念和0–3评分。历史内容仍可按源提交查看，不把90项旧练习强加给当前学员。
- 新增统一START-HERE入口与B02最小课包；代码里的概念提示同步当前编号。

整合提交同时以这两个来源为父提交，保留两边历史。不是用一个分支强制覆盖另一个，也不重写main历史。

## 分支清理

仅在main本次合并且自动检查成功后，尝试清理 `course/foundation-v1` 和 `course/interactive-mastery-v2`。清理工作流仅由带 `[consolidate-course]` 标记的本次主线提交触发；删除前验证分支头已属于main历史并重新核对头部。若存在新的未合并内容则跳过。实际结果看该次Actions日志，不预先声称已删除。

此后学习只看main，日常以小提交迭代。原PR描述和旧链接是历史记录，不代表当前课程结构或测试状态。
