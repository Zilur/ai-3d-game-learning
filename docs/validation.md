# Foundation v2 验证记录 · 2026-09-14

## 实际通过

`python tools/validate_repo.py`：Markdown相对链接、32概念/64题唯一ID和覆盖、答案覆盖、打印卡覆盖、Godot资源路径、星星glTF数据长度、120个平面着色顶点/40三角形、法线方向/长度、焊接后闭合边检查通过。

Python源文件编译检查通过；星星可用生成脚本重建。以上不是完整Khronos glTF验证器结果，也不是Godot导入结果。

Chromium + Playwright实际执行HTML脚本：初始世界位置、父/自身/世界三种位移、首次预测门槛、铰链/中心轴、Reset、390px窄屏无横向溢出、无JS运行错误。另检查桌面与窄屏截图。由于当前浏览器限制file://，自动化使用set_content加载相同HTML正文；没有验证用户机器的本地文件策略。

## 明确未测

本轮环境未安装Godot或Blender；尝试下载Godot受到网络/DNS限制。因此未完成GDScript引擎解析、glTF引擎导入、Godot烟雾测试、实际游玩、材质渲染、Blender导入、导出包和目标硬件性能测试。代码中的测试入口已提供，不把它记录为已通过。

没有调用OpenMAIC生成新版课堂。用户此前报告B01首版体验良好，但没有延迟测试数据；不能声称学习效果已被证实。

## 本地验收顺序

1. 按game/README导入，再执行smoke.gd；保留真实日志和版本。
2. 手工检查走跑跳、墙、平台、镜头极限、5颗星、完成/重开和坠落恢复。
3. 运行material_lab，检查单变量滑条、固定参考球、Emission与真实灯光差异。
4. Blender导入/改一个参数/导出/重导入，核对源资产与包装场景分工。
5. 新版B01在OpenMAIC生成一次，按互动清单验证，再安排迁移和隔次复习。

## 对v1的关键修订

四种知识标签收敛为按阶段变化的两种深度；Local拆清父/自身/世界；Origin与工具Pivot分开；加入Emission/Glow/照明区别、资源共享、防重复事件、帧间隔与GPU时间区别。加入高级可选支线，不强迫初学者过早学习。

不自动合并main，不自动生成收费课堂、不自动部署网页、不提交个人学习日志或密钥。
